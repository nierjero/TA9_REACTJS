Arquitectura de computadoras (Andrew Tanenbaum)

arquitectura(hardware), sistema operativo, Sistema gestor de base de datos y operativos(software).

Computadora
     I
     I
 Computan (procesan datos)

El CPU y la memoria RAM estan directamente conectados mediante un BUS.
La memoria esta compuesta por un programa y datos.
La memoria ROM, solo de lectura, tiene un programa (filmware) un software guardadoen el hardware.
La ROM se copia en la RAM(esta empieza vacía)

La CPU se divide en tres partes: ALU, registro y U.C.
Unidada aritmeticológica: donde se realizan los procesos matemáticos.

Unidad de control :Ejecuta linea por linea el programa 


TD: Familiarizarse con la placa ATMEGA 328p
Investigar computadora milimalista
microchip studio

Algebra de Boole:
Trabaja con 0 y 1, transformando valores del mundo real digitalizandolo con lenguaje binario.

Representación interna

Operación --- algebra de Boole.

Las operaciones existentes se basan en dos conjuntos {AB},{ + ,* , -}

+ : b**2 ---- b
0+0=
1+0=1
0+1=1   }or
1+1=1
* : b**2 ---- b
0*0=0
0*1=0
1*0=0  } and
1*1=1
- : b ---- b 
0 negado = 1
1 negado = 0    } not



